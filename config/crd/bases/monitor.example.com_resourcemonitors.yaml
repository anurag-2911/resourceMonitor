---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: resourcemonitors.monitor.example.com
spec:
  group: monitor.example.com
  names:
    kind: ResourceMonitor
    listKind: ResourceMonitorList
    plural: resourcemonitors
    singular: resourcemonitor
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourceMonitor is the Schema for the resourcemonitors API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ResourceMonitorSpec defines the desired state of ResourceMonitor
            properties:
              alertmanager:
                properties:
                  namespace:
                    type: string
                  serviceName:
                    type: string
                required:
                - namespace
                - serviceName
                type: object
              podThreshold:
                description: Number of pods to trigger alert
                type: integer
            required:
            - alertmanager
            - podThreshold
            type: object
          status:
            description: ResourceMonitorStatus defines the observed state of ResourceMonitor
            properties:
              podsCount:
                description: Define observed state of the cluster
                type: integer
            required:
            - podsCount
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
